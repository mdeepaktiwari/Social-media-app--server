let expressjwt,Post;_aa7‍.x([["requireSignIn",()=>requireSignIn],["canEditDeletePost",()=>canEditDeletePost]]);_aa7‍.w("express-jwt",[["expressjwt",["expressjwt"],function(v){expressjwt=v}]]);_aa7‍.w("../models/post",[["default",["Post"],function(v){Post=v}]]);


       const requireSignIn = expressjwt({
  secret: process.env.JWT_SECRET,
  algorithms: ["HS256"],
});

       const canEditDeletePost = async (req, res, next) => {
  try {
    const post = await Post.findById(req.params._id);
    // console.log(req.auth._id, post.postedBy);
    if (req.auth._id != post.postedBy) {
      return res.status(400).send("Unauthorized");
    } else {
      next();
    }
  } catch (e) {
    _aa7‍.g.console.log(e);
  }
};
 