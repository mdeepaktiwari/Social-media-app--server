let express,mongoose,cors,readdirSync;_622‍.w("express",[["default",["express"],function(v){express=v}]]);_622‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_622‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_622‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);



const morgan = require("morgan");
require("dotenv").config();
const router = require("./routes/auth");
const port = 8000;
const app = express();

mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
  })
  .then(() => {
    console.log("Connected to DB");
  })
  .catch((e) => {
    _622‍.g.console.log("Error in connected to DB", e);
  });
app.use(express.json({ limit: "5mb" }));
app.use(express.urlencoded({ extended: true }));
app.use(
  cors({
    origin: ["http://localhost:3000"],
  })
);

// autoload routes
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));
// app.use(morgan());

app.listen(port, () => _622‍.g.console.log("Server is running on port", port));
