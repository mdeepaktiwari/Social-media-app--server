let Post,cloudinary,User;_863‍.x([["createPost",()=>createPost],["uploadImage",()=>uploadImage],["postByUser",()=>postByUser],["userPost",()=>userPost],["updatePost",()=>updatePost],["deletePost",()=>deletePost],["newsFeed",()=>newsFeed],["likePost",()=>likePost],["unlikePost",()=>unlikePost],["addComment",()=>addComment],["removeComment",()=>removeComment],["totalPost",()=>totalPost],["posts",()=>posts]]);_863‍.w("../models/post",[["default",["Post"],function(v){Post=v}]]);_863‍.w("cloudinary",[["default",["cloudinary"],function(v){cloudinary=v}]]);_863‍.w("../models/user",[["default",["User"],function(v){User=v}]]);


// config cloudinary- we will save uploaded image to some cloud service provider
cloudinary.config({
  cloud_name: process.env.CLOUDINARY_NAME,
  api_key: process.env.CLOUDINARY_KEY,
  api_secret: process.env.CLOUDINARY_SECRET,
});

       const createPost = async (req, res) => {
  const { content, image } = req.body;
  if (!content) {
    return res.json({
      error: "Content is required",
    });
  }
  try {
    const post = new Post({ content, image, postedBy: req.auth._id });
    post.save();
    res.json(post);
  } catch (e) {
    _863‍.g.console.log("Error in creating the post", e);
    res.sendStatus(400);
  }
};

       const uploadImage = async (req, res) => {
  try {
    const result = await cloudinary.uploader.upload(req.files.image.path);
    return res.json({
      url: result.secure_url,
      public_id: result.public_id,
    });
  } catch (e) {
    _863‍.g.console.log("Error in uploading image to cloudinary", e);
  }
};

       const postByUser = async (req, res) => {
  try {
    // populate beacuse we dont want to show _id
    // sort to show the latest post
    // limit because we dont want to show all data to user at once
    const posts = await Post.find()
      .populate("postedBy", "_id name image")
      .sort({ createdAt: -1 })
      .limit(10);
    _863‍.g.console.log(posts);
    return res.json(posts);
  } catch (e) {
    _863‍.g.console.log("Error in finding the post the user", e);
  }
};

       const userPost = async (req, res) => {
  try {
    const post = await Post.findById(req.params._id)
      .populate("postedBy", "_id name image")
      .populate("comments.postedBy", "_id name image");
    return res.json(post);
  } catch (e) {
    _863‍.g.console.log(e);
  }
};

       const updatePost = async (req, res) => {
  try {
    const post = await Post.findByIdAndUpdate(req.params._id, req.body, {
      new: true,
    });
    res.json(post);
  } catch (e) {
    _863‍.g.console.log("Error in updating the post", e);
  }
};

       const deletePost = async (req, res) => {
  try {
    // take id from params passed from client side
    const post = await Post.findByIdAndDelete(req.params._id);

    // remove image from cloudinary
    if (post.image && post.image.public_id) {
      const image = cloudinary.uploader.destroy(post.image.public_id);
    }
    res.json({ ok: true });
  } catch (e) {
    _863‍.g.console.log("Error in deleting the post", e);
  }
};

       const newsFeed = async (req, res) => {
  try {
    const user = await User.findById(req.auth._id);
    let following = user.following;
    following.push(req.auth._id);

    // pagination
    // either new page but if nothing is there then show first page as default
    const currentPage = req.params.page || 1;
    const perPage = 3;

    const post = await Post.find({ postedBy: { $in: following } })
      .skip((currentPage - 1) * perPage)
      .populate("postedBy", "_id, name, image")
      .populate("comments.postedBy", "_id, name , image")
      .sort({ createdAt: -1 })
      .limit(perPage);
    res.json(post);
  } catch (e) {
    _863‍.g.console.log("Error in finding the post in news feed controller", e);
  }
};

       const likePost = async (req, res) => {
  try {
    const post = await Post.findByIdAndUpdate(
      // $addToSet not push because we want unique items
      req.body._id,
      {
        $addToSet: { likes: req.auth._id },
      },
      { new: true }
    );
    res.json(post);
  } catch (e) {
    _863‍.g.console.log("Error in liking the post", e);
  }
};

       const unlikePost = async (req, res) => {
  try {
    const post = await Post.findByIdAndUpdate(
      req.body._id,
      {
        $pull: { likes: req.auth._id },
      },
      { new: true }
    );
    res.json(post);
  } catch (e) {
    _863‍.g.console.log("Error in unliking the post", e);
  }
};

       const addComment = async (req, res) => {
  try {
    const { postId, comment } = req.body;
    _863‍.g.console.log(postId);
    const post = await Post.findByIdAndUpdate(
      postId,
      // $push not $addToSet because comment need not be unique
      {
        $push: { comments: { text: comment, postedBy: req.auth._id } },
      },
      { new: true }
    )
      .populate("postedBy", "_id name image")
      .populate("comments.postedBy", "_id name image");

    return res.json(post);
  } catch (e) {
    _863‍.g.console.log("Error in adding comment to the post", e);
  }
};

       const removeComment = async (req, res) => {
  try {
    const { postId, comment } = req.body;
    const post = await Post.findByIdAndUpdate(
      postId,
      {
        $pull: { comments: { _id: comment._id } },
      },
      { new: true }
    );

    res.json(post);
  } catch (e) {
    _863‍.g.console.log("Error in removing the comment", e);
  }
};

// total number of post
// required for pagination
       const totalPost = async (req, res) => {
  try {
    const total = await Post.find().estimatedDocumentCount();
    return res.json(total);
  } catch (e) {
    _863‍.g.console.log(e);
  }
};

       const posts = async 